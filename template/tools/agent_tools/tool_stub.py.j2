# {{ tool.name }}.py
# TODO: implement this UC function, then run the tools bundle to deploy it.

def {{ tool.name }}(
    # TODO: define your parameters with type hints, e.g.:
    # query: str,
) -> str:
    """
    {{ tool.description if tool.description else "TODO: describe what this function does." }}

    This function is deployed as a Unity Catalog Python function and called by the agent.
    TODO: implement the logic below.
    """
    raise NotImplementedError("{{ tool.name }} is not yet implemented.")
